<?xml version="1.0" encoding="UTF-8"?>

<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      http://doctrine-project.org/schemas/orm/doctrine-mapping.xsd"
>
    <!-- The recurring-charge consent a customer accepted on one order, with the exact text shown. -->
    <mapped-superclass name="JpmMartin\SyliusSubscriptionPlugin\Entity\SubscriptionConsent" table="jpm_martin_sylius_subscription_consent">
        <unique-constraints>
            <!-- One acceptance per order, held by the database: accepting twice replaces, never adds. -->
            <unique-constraint name="uniq_jpm_martin_sylius_subscription_consent_order" columns="order_id" />
        </unique-constraints>

        <id name="id" column="id" type="integer">
            <generator strategy="IDENTITY" />
        </id>

        <field name="textVersion" column="text_version" type="string" length="255" />
        <field name="text" column="text" type="text" />
        <field name="acceptedAt" column="accepted_at" type="datetime_immutable" />

        <many-to-one field="order" target-entity="Sylius\Component\Core\Model\OrderInterface">
            <join-column name="order_id" referenced-column-name="id" nullable="false" on-delete="CASCADE" />
        </many-to-one>
    </mapped-superclass>
</doctrine-mapping>
