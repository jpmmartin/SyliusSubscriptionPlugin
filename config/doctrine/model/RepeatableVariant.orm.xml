<?xml version="1.0" encoding="UTF-8"?>

<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      http://doctrine-project.org/schemas/orm/doctrine-mapping.xsd"
>
    <!-- A row per variant the store lets customers repeat; kept by the plugin, so the store's variant is left alone. -->
    <mapped-superclass name="JpmMartin\SyliusSubscriptionPlugin\Entity\RepeatableVariant" table="jpm_martin_sylius_subscription_repeatable_variant">
        <id name="id" column="id" type="integer">
            <generator strategy="IDENTITY" />
        </id>

        <one-to-one field="productVariant" target-entity="Sylius\Component\Core\Model\ProductVariantInterface">
            <join-column name="product_variant_id" referenced-column-name="id" nullable="false" unique="true" on-delete="CASCADE" />
        </one-to-one>
    </mapped-superclass>
</doctrine-mapping>
